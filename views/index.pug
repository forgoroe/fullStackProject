extends layout

block content
  h1= title
  p Welcome to #{title}
  br
  h5 Use the form below to visit a specific user page

  form(action="/users/submit" method="POST")
    input(type="text" name="id")
    button(type="submit") submit